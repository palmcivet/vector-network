export type Point = {
  x: number;
  y: number;
};

export type Segment = {
  start: Point;
  end: Point;
};

export type Rect = {
  left: number;
  top: number;
  right: number;
  bottom: number;
};
